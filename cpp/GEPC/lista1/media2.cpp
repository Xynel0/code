#include <bits/stdc++.h>
using namespace std;
int main(){
    double a;
    double b;
    double c;
    double media;
    cin >> a;
    cin >> b;
    cin >> c;
    media = (a*2 + b*3 + c*5) / 10;
    cout << "MEDIA = " << fixed << setprecision(1) << media << endl;
}
